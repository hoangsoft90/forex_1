# working.md — Trading Discipline OS (Phase 1)

> Nhật ký làm việc. Format: `- [YYYY-MM-DD] nội dung`.

## Tiến độ

- [2026-08-17] **🔍 Review toàn bộ code đợt Retention — fix 3 bug**: (1) 🔴 CRASH `cost-of-indiscipline.ts` — symbol ngoài 3 cặp (GBPUSD import MT4) → `pipValuePerLot(symbol as SymbolKey)` crash → thêm guard `isSupportedSymbol` + 2 test; (2) 🟡 `danger-zone.tsx` biểu đồ vi phạm hiện cả khi <30 lệnh (vi phạm tinh thần AC) → chỉ hiện khi đủ ngưỡng; (3) 🟡 `configureNotificationHandler` chưa được gọi → notification không hiện khi foreground → wire vào `(main)/_layout`. Verify 226/226 + tsc/lint/bundle OK.

- [2026-08-17] **🎉 XONG ĐỢT RETENTION LAYER (0-8)** — DoD: (1) M0 chưa đạt 95% thật → M3 giữ fallback 3.3, gate cứng không hạ ✓ (2) M1+M2 hoàn thành AC, route mặc định = Today Dashboard ✓ (3) M4-8 ngưỡng 30 lệnh + test tự động ẩn/hiện ✓ (4) Không feature Free nào bị chuyển Pro-only ✓ (5) Disclaimer Cost of Indiscipline có snapshot test ✓. Tổng 224/224 test. Chưa commit — diff gồm M0-8 + jest.setup + schema mục 13.

- [2026-08-17] **Retention Layer — Module 8 (Push Notification) XONG (module cuối đợt)**: lib `notification-content.ts` (morning brief score+rules / evening review CHỈ khi có lệnh đóng — ok:false nếu không; tone Auditor cân bằng — 8 test gồm 5 bộ dữ liệu + banned words). `notification-manager.ts` (opt-in đúng ngữ cảnh: hỏi permission SAU lần đầu thấy Dashboard qua AsyncStorage; local daily schedule). Settings: bật/tắt TỪNG loại + giờ HH:MM → upsert `notification_preferences` + reschedule. Schema thêm 2 bảng `notification_preferences` + `feature_flags` (seed INSTANT_AUDIT_ENABLED=false) vào schema.sql + data_model.md mục 13. app.json thêm plugin expo-notifications. Test 224/224 (+8). features.md cập nhật.

- [2026-08-17] **Retention Layer — Module 7 (Discipline Streak) XONG**: lib `src/lib/discipline-streak.ts` (streak theo lệnh, duyệt entry_time tăng dần rồi đếm từ lệnh gần nhất lùi dần; điều kiện followed=true + không vi phạm; reset khi gặp lệch plan/vi phạm). Wire Today Dashboard (Free, không gate — card 🔥). Test 216/216 (+7, gồm AC 8 lệnh → streak 8, lệnh 9 vi phạm → reset 0). features.md cập nhật.

- [2026-08-17] **Retention Layer — Module 6 (Personal Danger Zone) XONG**: mở rộng `src/lib/danger-zone.ts` — thêm `findNthOrderPattern` (vi phạm ở lệnh thứ N trong ngày, ngưỡng ≥30 + ≥5 lần) + `findDangerZoneDetail` (tổng hợp 2 pattern) + `nthOrderSummary`. Màn chi tiết `(main)/danger-zone` (route + nav grid + card dashboard link): pattern đầy đủ + biểu đồ phân bố vi phạm theo giờ (bar); dưới ngưỡng → thông báo tiến độ không kết luận. Test 209/209 (+4 nth-order). features.md cập nhật.

- [2026-08-17] **Retention Layer — Module 5 (Setup/Strategy Analytics) XONG**: lib `src/lib/setup-analytics.ts` (nhóm theo setup_tag, null/'other' → "Chưa phân loại"; Winrate/Avg R:R từ entry/sl/tp thật/Total PnL; ngưỡng ≥30 lệnh → progressText "Cần thêm N lệnh nữa (X/30)"; `bestSetupInsight` Pro — nhóm ≥5 lệnh). Màn hình `(main)/setup-analytics` (route + nav grid dashboard): Free bảng, Pro gợi ý câu. Test 205/205 (+8). features.md cập nhật.

- [2026-08-17] **Retention Layer — Module 4 (Cost of Indiscipline) XONG**: lib `src/lib/cost-of-indiscipline.ts` (công thức spec: hypothetical − actual; lệnh lệch plan thay bằng PnL tại planned_tp nếu đủ entry/sl/tp, thiếu → bỏ qua; ngưỡng ≥30 lệnh + ≥3 lệch plan; `COST_DISCLAIMER` cố định nguyên văn). Component `cost-of-indiscipline-card.tsx` dùng chung Dashboard + Weekly Audit, disclaimer ngay dưới con số, Free 1 dòng + Pro breakdown note. Test 197/197 (+14: 10 lib + 4 UI snapshot). ⚠️ GIẢ ĐỊNH: "lệnh lệch plan theo plan sẽ đạt planned_tp" — spec chưa ghi rõ cách tính PnL giả định, CẦN USER XÁC NHẬN. features.md cập nhật.

- [2026-08-17] **Retention Layer — Module 3 (Onboarding Instant Audit) XONG (fallback 3.3 active)**: `INSTANT_AUDIT_ENABLED=false` (gate cứng, lib `src/lib/instant-audit.ts`). Fallback 3.3: màn hình `(onboarding)/weakness-summary` liệt kê điểm yếu từ `weakness_profile` quiz (không gọi parser). Path khi flag bật: `(onboarding)/instant-audit` (dán lịch sử → parse-mt4 → Behavior Engine `computeInstantAudit` — reuse `detectViolations`, không logic mới; bỏ qua được; dòng lỗi hiện rõ + cảnh báo). Quiz điều hướng theo flag. Test 183/183 (+10). features.md cập nhật.

- [2026-08-17] **Retention Layer — Module 2 (Today Dashboard) XONG**: `(main)/index.tsx` = Today Dashboard (route mặc định, thay Journal — Journal còn là screen riêng trong nav grid). Nội dung Free đầy đủ: Discipline Score + delta tuần (snapshots), Danger Zone 1 dòng (ẩn <30 lệnh — logic `src/lib/danger-zone.ts`, Module 6 dùng chung), Rules active (RULE_TEMPLATES), lệnh đang mở (PnL tạm chờ nguồn giá Phase 3), Quick Plan nổi bật → Fast Plan, hướng dẫn user mới, nav grid. Test 173/173 (+8 danger-zone). features.md cập nhật.

- [2026-08-17] **Retention Layer — Module 1 (Fast Plan) XONG**: `new-plan.tsx` thành form rút gọn 5 trường bắt buộc (Symbol/Direction/Entry/SL/Risk%) hiển thị ngay; SL chặn cứng qua `src/lib/fast-plan.ts` (validateFastPlan + missingOptionalDetails, testable); Risk% prefill từ `max_risk_per_trade` (sửa được); TP/Thesis/Setup/Invalidation/Confidence gấp dưới "Chi tiết thêm"; R:R chỉ hiện khi có TP, Lot size tính đúng (công thức không đổi); trade-detail nhắc nhẹ không chặn khi thiếu Thesis/Setup/Confidence; analytics `fast_plan_opened`/`fast_plan_saved`. Test 165/165 (+9). features.md cập nhật.

- [2026-08-17] **Retention Layer — Module 0 (Fix & Verify MT4/MT5 Parser) — HARDENED, chưa verified thật**: parser (lib + edge parse-mt4, sync 1:1) đã harden: locale-aware số (comma decimal EU `1.100,50` / period), đa format ngày giờ (YYYY.MM.DD HH:MM / DD.MM.YYYY HH:MM / ISO), deal-based in/out (2 dòng = 1 lệnh đóng, close từ out-deal), skip balance/deposit/withdrawal có đếm (không phải lỗi), thêm alias cột (Position ID, Volume, Ticket). `raw_import_payload` giữ nguyên ở edge. `INSTANT_AUDIT_ENABLED` CHƯA tạo (Module 3 sẽ tạo, mặc định false). ⚠️ User CHƯA có export thật → chưa đạt 95% thật, gate Module 3 vẫn cứng. Test 15/15 parser, tổng 156/156 (+9). features.md đã cập nhật.

- [2026-08-17] **Module 0 xong**: scaffold Expo app (`apps/mobile`, SDK 57 TS + expo-router), supabase client (`src/lib/supabase.ts`), schema đầy đủ (`supabase/schema.sql` — 15 bảng, RLS, trigger adaptive decrease, indexes), script verify (`scripts/verify-connection.mjs`). OpenSpec change `phase1-mvp` valid (proposal + 9 specs + design + tasks). User đã tạo Supabase project, chạy schema, keys OK.
- [2026-08-17] **Module 1 xong + verified**: auth email/password, balance, quiz 7 câu → weakness_profile, explain Discipline vs Edge. Đã test THẬT trên Supabase (signUp/login, upsert profile, analytics, RLS) — pass, dọn sạch test data. Phát hiện: project đang bật "Confirm email" — user cần xác nhận email sau đăng ký.
- [2026-08-17] **Module 2 xong + verified**: `src/lib/trading-rules.ts`, màn hình constitution + constitution-settings, AuthProvider thêm hasRequiredRules. Test 12/12, test thật pass.
- [2026-08-17] **Module 3 xong + verified**: `src/lib/risk-engine.ts`, form `(main)/new-plan`, route thoát `(main)/confirm-no-plan`. Test 25/25, test thật pass.
- [2026-08-17] **Module 4 xong**: `src/lib/interruption.ts` (trigger over_risk/max_daily_loss/revenge_pattern + evidence 2 tầng cohort/personal, ưu tiên trigger), tích hợp vào new-plan (hiển thị trước khi lưu, ghi decision_interruptions với user_decision). Test 37/37.
- [2026-08-17] **Module 5 xong**: Mobile Widget `(main)/execution-widget` (nhập nhanh + auto-link plan + analytics opened/saved + nhập exit → auto-trigger compute-deltas), `(main)/paste-mt4` (paste history), `src/lib/mt4-parser.ts` + Edge Function `parse-mt4` (FORMAT GIẢ ĐỊNH chưa verify thật — 5 bộ test data giả lập desktop/mobile). Test 43/43.
- [2026-08-17] **Module 6 xong**: `src/lib/deltas.ts` (entry/sl/risk deviation + followed_plan ngưỡng 5 pip/0.2%/không SL adjust) + Edge Function `compute-deltas`, Journal `(main)/journal` (badge Theo plan/Lệch plan + insight ẩn nếu <10 lệnh), `(main)/trade-detail` (Planned vs Actual side-by-side). Test 50/50.
- [2026-08-17] **Module 7 xong**: `src/lib/violations.ts` + Edge Function `detect-violations` (overconfidence_size/revenge_trading/hope_trading/martingale_negative; news_gambling placeholder rõ chưa implement; ghi rule_violations không duplicate). Test 62/62.
- [2026-08-17] **Module 8 xong**: `src/lib/discipline-score.ts` (công thức Phase 1: adherence - min(violations×5,40), clamp 0-100; edge score winrate/avgRR/totalPnl; weekBounds), màn hình `(main)/scores` (2 trục điểm cạnh nhau + giải thích + Tiến bộ tuần này + tier gating Free/Pro + ghi snapshot 1 lần/tuần). Test 72/72.
- [2026-08-17] **Module 9 xong**: `src/lib/weekly-audit.ts` (template tiếng Việt, xử lý count=0, không LLM) + màn hình `(main)/weekly-audit` + Edge Function `weekly-audit`. Test 79/79.
- [2026-08-17] **PHASE 1 HOÀN THÀNH (code)**: 9/9 module, TSC/lint sạch, 79 test pass (42 test công thức ≥ 20 yêu cầu). Edge Functions đã viết nhưng CHƯA deploy (cần `supabase functions deploy` khi có keys). Còn chờ: deploy edge functions, verify MT4 format thật, đo AC thời gian thực.
- [2026-08-17] **Deploy Edge Functions XONG**: 4 functions (`parse-mt4`, `compute-deltas`, `detect-violations`, `weekly-audit`) đã deploy lên project `ycmuuczwnogybyklzpsa` + verify thật (parse 3 lệnh mẫu → DB, weekly-audit trả template, 401 khi thiếu JWT). **Nguyên nhân lỗi trước**: chạy CLI từ trong `supabase/` → CLI nhân đôi path thành `supabase/supabase/functions/...`. **Fix**: tạo `supabase/config.toml` (project_id + verify_jwt=true cho cả 4 fn) + chạy deploy từ root repo, xóa junk dir `supabase/supabase/`. Còn chờ: verify MT4 format thật, đo AC thời gian thực.
- [2026-08-17] **Chốt không confirm email**: `mailer_autoconfirm = true` (user_profiles đăng ký xong có session ngay). Phát hiện + fix lỗi "Email is invalid" = Supabase từ chối email có dấu tiếng Việt/khoảng trắng (ASCII only).
- [2026-08-17] **PHASE 2 XONG (4 module)**: OpenSpec change `phase2-mvp` (proposal + 4 specs + tasks). P2-M1 AdMob rewarded → Pro 24h (`react-native-google-mobile-ads` + `src/lib/admob.native.ts` web-stub + `pro-unlock.ts` + `tier.ts` + bảng `pro_unlocks` migration-phase2.sql + màn hình `(main)/pro`); P2-M2 TradingView chart (`tradingview-chart.native.tsx` WebView + web stub iframe, tích hợp new-plan + trade-detail); P2-M3 Portfolio Risk (`portfolio-risk.ts` tổng risk dồn + correlation ước lượng + màn hình có tier gating); P2-M4 Adaptive Rules ATR (`atr.ts` — chỉ GIẢM risk, trigger DB chặn tăng verified thật). Test 129/129 (50 mới), TSC/lint sạch, Android+web bundle OK.
- [2026-08-17] **Lưu ý Phase 2**: user cần chạy `supabase/migrations-phase2.sql` (bảng pro_unlocks) trên SQL Editor — chưa chạy thì `pro_unlocks` insert fail (quyền Pro vẫn OK). AdMob dùng test ad unit ID — cần AdMob account thật khi ra mắt. ATR/correlation là giá trị ước lượng tham chiếu (đánh dấu rõ trong UI), cần nguồn giá thật ở Phase 3.
- [2026-08-17] **Push lên GitHub + GH Actions build debug APK**: commit đầu tiên `1cd965c` (159 files, Phase 1+2) push lên `github.com/hoangsoft90/forex_1` (branch main). Workflow `.github/workflows/build-apk.yml` đổi sang **debug APK** (`assembleDebug`, debug keystore, no EAS token) → run #32009848642 `in_progress`. Tạo skill mới `.agents/skills/build-debug-apk-gh/SKILL.md` (repo link + cách dùng GH token từ env/chat, không hardcode token hết hạn). ⚠️ Token user dán trong chat cần revoke sau build xong.
- [2026-08-17] **Navigation rà soát toàn diện + fix điểm chết**: tạo `src/lib/navigation.ts` (safeBack — canGoBack→back, không thì replace fallback; áp dụng mọi nút back thủ công: confirm-no-plan, execution-widget, new-plan, pro, paste-mt4, constitution-settings). Fix `(main)/trade-detail` treo vĩnh viễn khi deep-link thiếu id (redirect về journal) + nút "Quay lại Journal" khi id không tồn tại. Thêm `+not-found.tsx` (route không tồn tại → màn hình 404 có lối thoát). Onboarding (quiz/explain/constitution) thêm nút quay lại bước trước (dùng replace — KHÔNG dùng safeBack vì replace không giữ stack → canGoBack sai → loop về login). Web export sạch warning (2 warning duplicate import đã gộp; SafeAreaProvider do expo-router tự wrap nên không cần thêm).
- [2026-08-17] **AdMob hoàn thiện (4 yêu cầu user)**: (1) `src/lib/ads-config.ts` — config file duy nhất: flag `TEST_ADS=true` mặc định (dùng test ad unit IDs của Google, tránh AdMob giới hạn tài khoản), `TEST_DEVICE_IDS` để đăng ký device khi dùng ID thật; banner + rewarded đều qua `getAdUnitId()`. (2) `src/components/ad-banner.native.tsx` + web stub — bottom banner dùng `useSafeAreaInsets().bottom` (không bị che bởi 3 nút Android; app.json set `navigationBar` transparent, edge-to-edge bắt buộc từ Android 16). (3) `expo-build-properties` `usesCleartextTraffic: true` — verify trong AndroidManifest sau prebuild (HTTP mọi domain hoạt động ở release APK). (4) `.github/workflows/build-apk.yml` — push/main → expo prebuild → gradle assembleRelease → upload APK (debug-signed, test ads, theo skill gh-actions-expo-apk-build). API fix: v16.4 dùng `MobileAds()` (không phải `mobileAds()`), size `BannerAdSize.BANNER` (không có `AdSize`).
- [2026-08-17] **Nâng targetSdk lên 36 + cooldown ads**: app.json `expo-build-properties` thêm `compileSdkVersion: 36`, `targetSdkVersion: 36`, `buildToolsVersion: 36.0.0` (Google Play yêu cầu API 36 từ 31/8/2026) — verified trong gradle.properties sau prebuild. Setup Android SDK local (`~/Android/Sdk`: cmdline-tools, platform-36, build-tools 36.0.0, platform-tools; ANDROID_HOME/ANDROID_SDK_ROOT vào shell config). Cooldown rewarded ads: `src/lib/ad-cooldown.ts` (AD_REWARD_COOLDOWN_MS=5 phút, `getRemainingCooldownMs`/`isCooldownActive`/`formatCooldown`/`getLastRewardedAt`/`recordRewardedAt` — AsyncStorage persist, fail-open nếu lưu lỗi), wire vào `pro-unlock.ts` (check cooldown trước khi hiện ad, ghi nhận sau khi rewarded) + UI `(main)/pro.tsx` (đếm ngược mỗi giây + disable nút + hint chống spam). Test 147/147 (18 cooldown mới, mock AsyncStorage in-memory tránh lint require).
- [2026-08-17] **Code review toàn diện + fix 4 bug chính**: (1) 🔴 `portfolio-risk.tsx` VÒNG LẶP VÔ HẠN — `setSymbols(mảng mới)` trong `useCallback([user, symbols])` → effect chạy lại mãi, app treo + spam Supabase; fix: bỏ `symbols` khỏi deps + dùng biến local `uniqueSymbols` cho correlationMatrix. (2) `constitution.tsx` + `constitution-settings.tsx` — `canAddRule(activeCount + 1)` chặn luôn lần thứ 3 → Free không thêm được luật tùy chọn; fix: `canAddRule(activeCount)`. (3) `scores.tsx` + `weekly-audit.tsx` — query `plan_vs_reality_deltas` chỉ filter user_id → adherence/% theo plan tính trên TOÀN LỊCH SỬ; fix: thêm `id` vào select executions + lọc delta theo `trade_execution_id` của tuần. (4) `confirm-no-plan.tsx` — insert execution rác (lot 0/entry 0) tạo lệnh ma vĩnh viễn trong Journal + Portfolio Risk; fix: xác nhận → `router.replace('/(main)/execution-widget')` nhập lệnh thật. Đã report thêm 4 bug nhỏ chưa fix (user chọn chỉ fix 4 chính): text "Module 2 sẽ được build" lỗi thời ở index, tier stale sau signOut (auth-context), plan status không set 'executed' khi link widget, dead code navigateWithFallback. Verify: TSC 0, lint 0, 147/147 test, bundle android+web OK.
- [2026-08-17] **Tạo .project/ Knowledge Item**: 8 files (README entry point, overview — mục đích/stack/cấu trúc, architecture — luồng dữ liệu/15 bảng/4 edge functions/công thức, patterns — quy ước navigation/fetch/tier/ads/platform/test, state — tiến độ/todo/bug/decision log, ai-rules — rule bắt buộc cho agent, modules/phase1 + phase2 — tóm tắt 9+4 module). Tổng 445 dòng. Chưa commit.
- [2026-08-17] **Tạo memory files root**: điền phần PROJECT trong `AGENTS.md` (Role & Context, Initial Setup, 9 Critical Rules, Workflow, Branch/Commit convention) + `CLAUDE.md` là **symlink → AGENTS.md** (tránh 2 bản lệch). Tạo mới `context.md` (tổng quan tĩnh — tham chiếu .project/ thay vì lặp) + `operating_rules.md` (17 rule riêng project, không lặp AGENTS.md). `working.md` đã có sẵn, giữ nguyên. Để session sau mở lại đọc: AGENTS.md/CLAUDE.md (session start) → context.md → working.md → operating_rules.md → .project/ nếu cần sâu. Chưa commit.
- [2026-08-17] **OpenSpec update — archive cả 2 change**: `openspec archive` chạy thành công → sync delta specs vào main specs (lần đầu tạo `openspec/specs/` với 13 specs: 9 Phase 1 + 4 Phase 2, tổng 50 requirements, validate 13/13 pass) + move change vào `openspec/changes/archive/` (2026-08-17-phase1-mvp, 2026-08-17-phase2-mvp). Active changes = 0. Chưa commit.
- [2026-08-17] **Fix GH Actions build APK (run #32009848642 failure) + push + rebuild**: lỗi `:react-native-google-mobile-ads:compileDebugKotlin FAILED` — play-services-ads 25.4.0 (RNGMA 16.4.0 kéo về) compile bằng Kotlin 2.3 metadata, RN/Expo SDK 57 dùng Kotlin 2.1. Fix theo skill gh-actions-expo-apk-build (không bump Kotlin — có thể crash lib khác): (1) pin `play-services-ads:24.2.0` (Kotlin 2.1-safe) qua config plugin `plugins/pin-gradle-dependency.js` đăng ký trong app.json (verified trong android/build.gradle sau prebuild); (2) patch-package RNGMA 16.4.0 — bỏ `AgeRestrictedTreatment` (import + block ageRestrictedTreatment, app không dùng) + thay `getLargeAnchoredAdaptiveBannerAdSize` (25.x-only) bằng `getCurrentOrientationAnchoredAdaptiveBannerAdSize` (dead path, app chỉ dùng BannerAdSize.BANNER) → `patches/react-native-google-mobile-ads+16.4.0.patch` (49 dòng), postinstall: patch-package, dry-run pristine OK. Warning "No android_app_id" = GIẢ (config plugin inject manifest meta-data, verified `APPLICATION_ID` trong AndroidManifest). Commit `41c3261` (62 files — gom toàn bộ diff treo: fix review, targetSdk 36, cooldown, .project/, memory files, openspec archive) push lên main → run `#32011501038` in_progress. TSC/lint/jest vẫn sạch.
- [2026-08-17] **OpenSpec update — đợt Retention Layer**: tạo change `retention-layer` (proposal.md + design.md + tasks.md 10 mục [x] + 9 delta specs: mt4-parser, fast-plan, today-dashboard, onboarding-instant-audit, cost-of-indiscipline, setup-analytics, personal-danger-zone, discipline-streak, push-notification) → validate pass → `openspec archive retention-layer --yes` → sync +41 requirements vào main specs (22 specs tổng, 91 requirements) + move vào archive `2026-08-17-retention-layer`. Active changes = 0. Chưa commit.
- [2026-08-17] **Cập nhật .project/ cho đợt Retention** (theo ai-rules): tạo `modules/retention.md` (9 module 0–8 + kiểm chứng) · README (TL;DR 3 giai đoạn, 226 test, việc treo → can_lam.md) · state.md (tiến độ + todo mới: SQL mục 13, redeploy parse-mt4, revoke token; 3 bug review mới; +9 decision: gate INSTANT_AUDIT, ngưỡng 30, giả định M4 chưa duyệt, local notification...) · architecture.md (17 bảng, 7 công thức mới) · patterns.md (feature flag từ DB fail-closed, ngưỡng 30, guard symbol, jest.setup AsyncStorage, UI snapshot test + act) · overview.md (226 test, 3 change archive, cấu trúc thư mục mới) · ai-rules.md (226 test). Chưa commit.
- [2026-08-17] **Fix app không mở được trên phone + rebrand**: chẩn đoán qua adb (Pixel 3a wireless): debug APK từ GH Actions `assembleDebug` KHÔNG nhúng JS → chết ngay "Unable to load script" khi không có Metro. Fix: plugin mới `apps/mobile/plugins/embed-js-in-debug.js` (withAppBuildGradle chèn `bundleInDebug = true` vào react block — verified qua prebuild local, applicationId='com.trademind.trading', app_name='Trading Discipline OS'). app.json: name "Trading Discipline OS", android.package `com.hoangsoft90.mobile`→`com.trademind.trading`, ios.bundleIdentifier `com.trademind.trading`, đăng ký plugin. .gitignore thêm `/apps/mobile/android/` + `/apps/mobile/ios/` (prebuild-generated, CI tự sinh). Verify TSC 0 / lint 0 / 226 test. Commit `d56ae6a` push → GH Actions run #32036619146 (queued). Chưa cài APK mới lên phone (chờ build xong).
- [2026-08-17] **Tạo skill mới từ bài học "code xong đi sửa"**: (1) `.agents/skills/apk-standalone-on-device/SKILL.md` (mới) — chẩn đoán "Unable to load script" (debug APK thiếu JS bundle), fix bundleInDebug qua plugin embed-js-in-debug, flow test adb + design-time checklist (chốt standalone/identity/native config trước build đầu tiên). (2) Cập nhật `build-debug-apk-gh` — thêm mục ⚠️ BẮT BUỘC nhúng JS bundle + identity mới `com.trademind.trading`. (3) `.project/ai-rules.md` rule 16 — nhắc skill mới + identity mới. Chưa commit (đi cùng diff đợt fix rebrand).
- [2026-08-17] **Fix build CI lần 2 (bài học "code xong đi sửa" lặp lại)**: run đầu (d56ae6a) FAIL 1m54s — `Could not set unknown property 'bundleInDebug' for extension 'react'` — RN 0.86/Expo 57 ĐÃ XÓA `bundleInDebug` khỏi ReactExtension; property đúng là `debuggableVariants` (list variant KHÔNG bundle, mặc định ['debug','debugOptimized'], set `=[]` = mọi variant bundle — verify trong `node_modules/@react-native/gradle-plugin/.../ReactExtension.kt` + TaskConfiguration.kt). Plugin `embed-js-in-debug.js` sửa sang `debuggableVariants = []`; guard đổi sang marker comment `// bundle JS into the debug APK` vì file sinh ra ĐÃ có comment mẫu `// debuggableVariants = [...]` làm guard theo tên property = no-op. Commit `4cad0c2` push → run #32037020242, config phase pass (vượt mốc 1m54s), build full đang chạy. Skills đã cập nhật API đúng + bước verify ReactExtension.kt (`.agents/` gitignored — chỉ local).
- [2026-08-17] **Đổi tên + tổng quát hóa 2 skills (theo yêu cầu user)**: `.agents/skills/apk-standalone-on-device` → **`expo-apk-standalone`** (áp dụng chung mọi project Expo/RN: bảng property theo RN version, template plugin generic, verify ReactExtension.kt, flow adb, design checklist — repo này chỉ là ví dụ). `.agents/skills/build-debug-apk-gh` → **`expo-build-debug-apk-gh`** (phần 1-3 generic Expo, phần 4 là thông tin repo Trading Discipline OS). Cập nhật reference: AGENTS.md (2 chỗ), .project/overview.md, .project/ai-rules.md (rule 16 + bundleInDebug→debuggableVariants). `.agents/` gitignored — chỉ local.
- [2026-08-17] **Push 2 skills mới lên repo `hoangsoft90/ai_skills` (commit `88fd571`, nhánh main)**: `skills/expo-apk-standalone/` (generic sẵn — mọi project Expo) + `skills/expo-build-debug-apk-gh/` (bản PUSH ĐÃ BỎ phần đặc thù repo Trading Discipline OS — chỉ giữ generic, đúng yêu cầu "áp dụng chung"). Dùng git clone https + token fine-grained mới (github_pat_...) của user. Bản local `.agents/` giữ nguyên (có mục repo-specific hữu ích cho project này). Không có file index/registry cần cập nhật.
- [2026-08-18] **Debug lỗi "ko đăng ký được" trên phone thật**: 2 nguyên nhân. (1) APK build từ GH Actions dùng **placeholder Supabase URL** (`https://placeholder.supabase.co` fallback trong supabase.ts) vì `.env` gitignored + workflow không set env → signup gửi tới domain không tồn tại → lỗi. Fix: inject `EXPO_PUBLIC_SUPABASE_URL/ANON_KEY/ADMOB_*` vào workflow env (public-by-design — đi kèm trong APK, lớp bảo vệ thật là RLS; dùng `secrets.X || 'fallback'` để user có thể override sau). Commit `af62242` → run #32085365417. (2) Schema chưa đầy đủ trên project thật: 16 bảng đã có nhưng thiếu `notification_preferences` + `feature_flags` (schema.sql rollback toàn bộ vì `user_profiles` đã tồn tại, không có IF NOT EXISTS). Fix: trích phần retention (dòng 465-500) chạy riêng qua Management API (curl + User-Agent `curl/8.0 supabase-cli` vì Cloudflare chặn UA mặc định của urllib — lỗi 1010). Đã xác nhận: feature_flags `INSTANT_AUDIT_ENABLED=false` ✓, notification_preferences tồn tại ✓, PostgREST cache refresh thấy đủ 19 bảng ✓. Signup server-side test OK (mailer_autoconfirm=true → session trả về ngay, email_confirmed_at có giá trị).
- [2026-08-18] **Redeploy edge `parse-mt4` (bản hardening M0, commit `cea495f` 10:13 UTC > lần deploy cũ 07:31)**: `supabase functions deploy parse-mt4` từ root → VERSION 2 ACTIVE (2026-08-18 01:34 UTC). Smoke test THẬT bằng user access token (không phải anon key — function gọi `auth.getUser(jwt)` nên anon key bị 401, đó là đúng thiết kế; app thật gửi user token qua supabase-js): signup user tạm → invoke → `{"imported":3,"errorLines":[],"skippedNonTrade":0,"detectedLocale":"periodDecimal"}` (3 mẫu EURUSD/USDJPY/XAUUSD parse sạch) → dọn sạch test data (executions 204 + user 200, cả user tạm bỏ lại từ lần chạy hỏng script UID-readonly). PostgREST + Auth + key anon trong .env đều còn valid. Xong 1 việc trong can_lam.md.
- [2026-08-18] **Verify SQL mục 13 + migrations-phase2 trên project thật (qua Management API, curl + UA `curl/8.0 supabase-cli`)**: **KHÔNG còn gì thiếu — 18/18 bảng đầy đủ**. Kết quả: `notification_preferences` (7 cột đúng schema, RLS on, 3 policies select/insert/update own) ✓ · `feature_flags` (flag_name PK, RLS on, policy select_all, seed `INSTANT_AUDIT_ENABLED=false`) ✓ · `pro_unlocks` ĐÃ TỒN TẠI (cột đủ + check constraint `method='admob_rewarded'` + FK user_id cascade + RLS on + 4 policies + index `idx_pro_unlocks_user_granted`) — migrations-phase2 đã được chạy lúc nào đó, doc cũ ghi "CHƯA" là lỗi thời ✓ · trigger adaptive `trg_adaptive_condition_decrease` ✓ · 7 index mục 12 ✓ · MỌI bảng public đều RLS on + có policy (0 bảng thiếu). Đã cập nhật state.md / modules/retention.md / README.md / can_lam.md (mục 1,2 đánh done) cho khớp thực tế.
- [2026-08-18] **Tạo icon app Trading Discipline OS (concept Shield + Candlestick)**: nền gradient navy (#123A6E→#06142A) + radial glow brand blue, khiên trắng viền navy, 3 nến xanh/đỏ (#16C784/#F6465D) đi lên + trend line + arrowhead (kỷ luật bảo vệ lệnh giao dịch). Render bằng PIL (supersample 4x, verify pixel: gradient ✓ shield ✓ nến ✓ trend ✓). Sinh: `assets/images/icon.png` (1024 RGB opaque — dùng chung iOS) · `android-icon-background.png` (512 gradient) · `android-icon-foreground.png` (512 logo transparent) · `android-icon-monochrome.png` (432 ring trắng + nến — themed icon) · `favicon.png` (48). app.json: `ios.icon` `./assets/expo.icon` → `./assets/images/icon.png` (bỏ icon Expo mặc định), `adaptiveIcon.backgroundColor` `#E6F4FE` → `#0D2B52`. Validate: `expo config --type public` parse OK, không còn ref expo.icon. Preview: /tmp/icon-preview.png. Cosmetic — không đụng logic, không cần test.
- [2026-08-18] **Rút bài học "app ko đăng ký được" → cập nhật skill `expo-build-debug-apk-gh`** (bài học generic, áp dụng mọi project Expo có backend, không chỉ repo này): section mới **1b ⚠️ BẮT BUỘC inject env CÔNG KHAI (`EXPO_PUBLIC_*`) vào workflow** — vì `.env` gitignored nên CI build không có env → `EXPO_PUBLIC_*` inline lúc bundle bị rỗng → rơi vào placeholder (`https://placeholder.supabase.co`) → APK build OK + mở được nhưng signup/login/fetch đều fail trên máy thật (không báo lỗi build). Fix: inject `EXPO_PUBLIC_*` vào `env:` workflow với `secrets.X || 'fallback'` (public-by-design — nằm trong APK, lớp bảo vệ thật là RLS; TUYỆT ĐỐI không inject secret thật vào APK). Kèm verify trước push (grep EXPO_PUBLIC_ trong workflow) + chẩn đoán nhanh (`strings app-debug.apk | grep -iE "placeholder|localhost"`). Cập nhật cả mục 3 (bước chuẩn workflow có env:) + mục 4 (env cụ thể của repo này). **Đã push lên repo `hoangsoft90/ai_skills` (commit `8126a1f4`, nhánh main) qua GitHub Contents API** — bản push = bản local BỎ phần repo-specific (mục 4, frontmatter description + intro đổi generic), verify trên remote: 133 dòng, section 1b ✓, không còn string repo ✓. Token user cấp mới (`github_pat_...`) — đã nhắc user revoke sau khi dùng xong.
- [2026-08-18] **Fix 4 bug nhỏ từ code review 2026-08-17** (3 fix + 1 xác nhận đã hết): (1) `index.tsx` text "Module 2 sẽ được build..." — **không còn tồn tại** (index.tsx đã rewrite thành Today Dashboard, grep toàn repo sạch) — không cần sửa. (2) `auth-context.tsx`: clear `tier` + `subscriptionExpiresAt` trong nhánh session null của `onAuthStateChange` (trước chỉ clear `onboarding`) → hết lỗi tier cũ hiện vài giây khi login tài khoản khác. (3) `execution-widget.tsx`: sau khi insert execution có `trade_plan_id` → update `trade_plans.status='executed'` (RLS `trade_plans_update_own` verified trong schema; fail chỉ `console.warn` không chặn luồng — đúng pattern phụ-write của project) → hết lỗi plan bị suggest lại nhiều lần. (4) `navigation.ts`: xóa `navigateWithFallback` dead code (verify 0 caller trước khi xóa). Verify: TSC 0 · lint 0 · **226/226 test pass** · code review (OCR không cài → fallback review mặc định: diff 3 file không có vấn đề, không thuộc vùng rủi ro Ponytail; impact: chỉ 3 file, caller auth-context bị ảnh hưởng đúng hướng fix; dead-code check pass). Chưa commit (nằm trong diff treo tổng).
- [2026-08-18] **Triển khai app-i18n (đa ngôn ngữ vi + en) — OpenSpec change `openspec/changes/app-i18n/`** (planning đã chốt trước: user giảm scope từ 6 → 2 ngôn ngữ, kiến trúc mở rộng được). Tasks 1-6 XONG:
  - **Hạ tầng**: cài `i18next` + `react-i18next` + `expo-localization` (plugin tự thêm vào app.json) · `src/i18n/index.ts` (SUPPORTED_LANGS=['vi','en'], resolveInitialLanguage: preference AsyncStorage → device → fallback vi, changeAppLanguage persist) · I18nGate trong root `_layout.tsx` · jest.setup: require('@/i18n') khởi tạo lng:'vi' cho mọi test + mock expo-localization.
  - **Màn hình (28)**: login, balance, quiz, weakness-summary, instant-audit, explain, constitution, +not-found, settings (**section chọn ngôn ngữ** — đánh dấu ngôn ngữ đang dùng), index, journal, trade-detail, confirm-no-plan, execution-widget, paste-mt4, new-plan, scores, weekly-audit, setup-analytics, danger-zone, portfolio-risk, pro, constitution-settings, discipline-explainer, _layout (main), cost-of-indiscipline-card, tradingview-chart.native (locale widget + message). ad-banner không có text.
  - **Nội dung động (lib, dùng i18next.t headless — KHÔNG đổi signature)**: weekly-audit, notification-content + notification-manager, weakness-quiz (localizeQuestion), instant-audit (WEAKNESS_LABELS → keys), danger-zone (summary/nthOrderSummary), cost-of-indiscipline (COST_DISCLAIMER thành hàm + hiddenReason), setup-analytics (labels/progress/insight), interruption (COHORT_BENCHMARKS thành hàm + personal evidence), mt4-parser (error reasons), fast-plan, atr, trading-rules (RULE_TEMPLATES → keys), admob (native+web), pro-unlock, tier (formatHoursLeft).
  - **Edge parse-mt4**: nhận `lang` body (mặc định vi), dictionary vi/en cho success + errorLines reason; client paste-mt4 (đã có) + instant-audit (thêm) truyền lang. **Đã redeploy + smoke test**: vi "Import 3 lệnh thành công..." · en "Imported 3 trades..." — dọn sạch test data.
  - **Test**: 226 cũ giữ pass (jest lng vi) + i18n base 9 test + **i18n-content-en.test.ts mới (7 test** — weekly audit/notification/disclaimer/interruption benchmark/instant audit/formatHoursLeft bằng tiếng Anh, không lẫn tiếng Việt) = **242/242** · TSC 0 · lint 0 · `expo export` android + web pass.
  - Còn lại Task 7: docs (working.md này + state.md decision log đã ghi) + Code Review theo AGENTS.md + ADR. Chưa commit (nằm trong diff treo tổng).
